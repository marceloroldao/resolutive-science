# v0.5 T46 — Direct P_B-Matched Intervention Protocol

**Status:** FROZEN BEFORE EXECUTION  
**Parent:** T45. **New dynamics:** none.

## 1. Question

T45 found that branch-core pair connectivity P_B is a stable parent-null discriminator but failed the strong within-null correlation gate.

T46 asks a stronger causal-accounting question inside the tree model:

> If alternative trees are forced to match the parent not only in exact degree sequence and C/A/G, but also in P_B, do the held-out high-radius responses M3 and M7 remain extreme?

No additional topology variable is introduced.

## 2. Held-out matrix

Use exactly:

```text
N = 2048
parent seeds = {389,419,449}
```

These parent graphs were not used in T25-T45.

No rescue seed may be added after execution.

## 3. Stage A — frozen baseline calibration

For each parent, first construct exactly one ordinary T41 ensemble:

```text
99 C/A/G-matched degree-preserving null trees.
```

Use the exact T41 settings:

- exact labeled degree sequence;
- tree class;
- C/A/G tolerance = 0.50 each in T40 baseline-null SD units;
- burn-in = 500 accepted moves;
- thinning = 50 accepted moves;
- 99 stored nulls;
- maximum proposals = 250000;
- parent-edge overlap <= 0.90.

M3 and M7 are forbidden from Stage A.

Compute P_B only after these 99 baseline nulls are frozen.

Let

```text
sigma_PB = sd(P_B over the 99 Stage-A nulls).
```

Define the intervention tolerance

```text
Delta_PB = 0.50 * sigma_PB.
```

The factor 0.50 is frozen before execution.

If sigma_PB = 0, the intervention requires exact P_B equality.

## 4. Stage B — P_B-matched intervention ensemble

Start a new independent degree-preserving Prüfer random-transposition walk from the exact parent Prüfer sequence.

A proposal is accepted only if all of the following hold:

```text
|C - C_parent| <= 0.50
|A - A_parent| <= 0.50
|G - G_parent| <= 0.50
|P_B - P_B(parent)| <= Delta_PB.
```

C/A/G use the same parent-specific T40 baseline standardization.

P_B is evaluated directly in its dimensionless scale.

The Stage-B random seed is a deterministic function of N and parent seed distinct from the Stage-A/T41 seed stream.

## 5. Stage-B schedule

Frozen per parent:

```text
burn-in accepted moves = 500
thinning accepted moves = 50
stored intervention nulls = 99
maximum proposals = 500000
parent-edge overlap maximum = 0.90
```

The proposal ceiling is doubled relative to T41 because P_B is an additional frozen constraint.

No tolerance or schedule may be changed after execution.

## 6. Held-out responses

Only after the 99 Stage-B nulls are frozen compute:

```text
M3 = median nu_4 for 4->8
M7 = mean existing-edge D_J at R=8.
```

M3 and M7 are forbidden from both Stage A and Stage B acceptance.

For each response report:

- parent value;
- Stage-B mean and sd;
- q025;
- median;
- q975;
- parent percentile;
- classification LOW / inside / HIGH.

## 7. Control diagnostics

For every stored Stage-B null verify:

1. exact labeled degree sequence;
2. connected tree with N-1 edges;
3. C/A/G matching;
4. P_B matching;
5. unique edge set;
6. parent-edge overlap <=0.90.

Also report realized P_B parent value, Stage-A sigma_PB, Delta_PB and Stage-B P_B range.

## 8. Gates

### T46_BASELINE_FEASIBILITY

PASS only if Stage A produces 99 valid T41 nulls for all three parents.

### T46_INTERVENTION_FEASIBILITY

PASS only if Stage B produces 99 valid intervention nulls for all three parents within 500000 proposals.

### T46_PB_MATCH_CONTROL

PASS only if every stored Stage-B null satisfies the frozen P_B tolerance.

### T46_HIGH_RADIUS_ACCOUNTING

IDENTIFIED only if all control gates pass and both parent responses

```text
M3 and M7
```

lie inside [q025,q975] for all three Stage-B ensembles.

### T46_RESIDUAL_SPECIFICITY_AFTER_PB

IDENTIFIED only if all control gates pass and

```text
M3_parent > q975
M7_parent > q975
```

for all three parents.

### Mixed result

Any other pattern is recorded exactly without rescue.

## 9. Interpretation boundary

If T46_HIGH_RADIUS_ACCOUNTING is identified:

> P_B materially accounts for the previously replicated residual within the current tree model under the frozen intervention design.

If T46_RESIDUAL_SPECIFICITY_AFTER_PB is identified:

> Exact degree + C/A/G + P_B are still insufficient to account for the residual.

Neither outcome establishes a physical law, spacetime, physical geometry, dimension, energy, time or dynamics.

## 10. Forbidden

- any topology variable beyond C/A/G/P_B;
- M3/M7 in Stage A or Stage B acceptance;
- tolerance relaxation;
- rescue seeds;
- changed branch threshold;
- physical target data;
- graph dynamics.

## 11. Frozen ledger before execution

```text
T46_PARENT = T45
T46_HOLDOUT_N = 2048
T46_HOLDOUT_SEEDS = {389,419,449}

T46_STAGE_A = EXACT_T41_BASELINE
T46_STAGE_A_NULLS = 99

T46_INTERVENTION_VARIABLE = P_B
T46_PB_TOLERANCE = 0.50 * STAGE_A_SD_PB

T46_STAGE_B_BURNIN_ACCEPTED = 500
T46_STAGE_B_THIN_ACCEPTED = 50
T46_STAGE_B_NULLS = 99
T46_STAGE_B_MAX_PROPOSALS = 500000
T46_PARENT_EDGE_OVERLAP_MAX = 0.90

T46_M3_M7_IN_SAMPLER = FORBIDDEN
T46_EXTRA_TOPOLOGY_VARIABLES = FORBIDDEN
T46_RESCUE_SEEDS = FORBIDDEN

T46_GRAPH_EVOLUTION = NONE
T46_TRANSITION_LAW = NOT_DERIVED
T46_PHYSICAL_GEOMETRY = NOT_DERIVED
```
