# v0.5 T46 — Direct P_B-Matched Intervention Results

**Protocol:** `v0.5_T46_DIRECT_PB_MATCHED_INTERVENTION_PROTOCOL.md`  
**Parent:** T45. **New dynamics:** none.  
**Hold-out matrix:** N=2048; seeds {389,419,449}.  
**Workflow run:** 36092491957.

## 1. Executive result

T46 directly matched the T45 discriminator P_B in addition to the existing exact-degree and C/A/G controls.

Both stages were feasible for all three held-out parents:

```text
T46_BASELINE_FEASIBILITY = PASS_3_OF_3
T46_INTERVENTION_FEASIBILITY = PASS_3_OF_3
T46_PB_MATCH_CONTROL = PASS
```

After P_B matching, both held-out responses remain above the Stage-B q97.5 boundary for every parent:

```text
M3 HIGH = 3/3
M7 HIGH = 3/3
```

Therefore:

```text
T46_HIGH_RADIUS_ACCOUNTING = NOT_IDENTIFIED
T46_RESIDUAL_SPECIFICITY_AFTER_PB = IDENTIFIED_3_OF_3
```

P_B is insufficient to account for the replicated high-radius residual.

## 2. Stage-A baseline calibration

Each parent first generated 99 ordinary T41 C/A/G-matched nulls.

| seed | Stage-A proposals | stored | sigma(P_B) | Delta P_B |
|---:|---:|---:|---:|---:|
| 389 | 10049 | 99 | 0.006211 | 0.003105 |
| 419 | 10753 | 99 | 0.006689 | 0.003344 |
| 449 | 9426 | 99 | 0.004377 | 0.002188 |

The intervention tolerance was frozen as

```text
Delta P_B = 0.50 * Stage-A sd(P_B).
```

No M3/M7 value entered Stage A.

## 3. Stage-B intervention feasibility

The second independent constrained walk required exact degree + C/A/G + P_B matching.

| seed | Stage-B proposals | accepted | stored | controls |
|---:|---:|---:|---:|:---:|
| 389 | 10265 | 5450 | 99 | PASS |
| 419 | 10583 | 5450 | 99 | PASS |
| 449 | 9967 | 5450 | 99 | PASS |

For all 297 Stage-B trees:

```text
degree_control = true
tree_control = true
cag_control = true
pb_control = true
diversity_control = true
```

Thus the additional P_B constraint does not make the intervention ensemble infeasible.

## 4. Realized P_B matching

| seed | parent P_B | Stage-B mean | Stage-B min | Stage-B max |
|---:|---:|---:|---:|---:|
| 389 | 0.052252 | 0.051400 | 0.049193 | 0.055272 |
| 419 | 0.027909 | 0.026785 | 0.024590 | 0.031094 |
| 449 | 0.057429 | 0.057212 | 0.055281 | 0.059593 |

The parent P_B value lies inside the realized Stage-B range in all three cases.

Therefore this is a genuine P_B-matched intervention rather than an extrapolation.

## 5. Held-out M3 after P_B matching

| seed | parent M3 | Stage-B mean | sd | q975 | class |
|---:|---:|---:|---:|---:|:---:|
| 389 | 0.666667 | 0.642621 | 0.003921 | 0.650000 | HIGH |
| 419 | 0.659091 | 0.641112 | 0.004791 | 0.652174 | HIGH |
| 449 | 0.666667 | 0.642116 | 0.004173 | 0.649257 | HIGH |

Approximate standardized parent-minus-null effects:

```text
seed 389 : +6.13 sd
seed 419 : +3.75 sd
seed 449 : +5.88 sd
```

## 6. Held-out M7 after P_B matching

| seed | parent M7 | Stage-B mean | sd | q975 | class |
|---:|---:|---:|---:|---:|:---:|
| 389 | 0.219351 | 0.191617 | 0.002644 | 0.199887 | HIGH |
| 419 | 0.210873 | 0.189659 | 0.003386 | 0.195149 | HIGH |
| 449 | 0.221063 | 0.191920 | 0.002572 | 0.197546 | HIGH |

Approximate standardized parent-minus-null effects:

```text
seed 389 : +10.49 sd
seed 419 : +6.26 sd
seed 449 : +11.33 sd
```

These standardized departures are descriptive effect sizes, not Gaussian p-values.

## 7. Main inference

T45 established:

```text
P_B = stable parent-null discriminator.
```

T46 establishes:

```text
matching P_B does not remove the M3/M7 residual.
```

Therefore:

```text
P_B = STRUCTURALLY_RELEVANT_BUT_INSUFFICIENT
```

The missing source is not reducible to:

- exact labeled degree sequence;
- tree class;
- global compactness C;
- degree assortativity A;
- betweenness concentration G;
- branch-core pair connectivity P_B.

## 8. Generator-level structural clue

The T25 generator has a stronger property that the previous nulls do not preserve.

Every new vertex v is attached to exactly one earlier vertex u<v.

Thus, in the original T25 label/birth order:

```text
for every v > 0:
number of neighbors u with u < v = 1.
```

Repeated parent links therefore define a strictly decreasing ancestral chain ending at vertex 0.

The generator also chooses the parent from a random sample of up to five older vertices, preferring the currently lowest-degree candidate.

The previous degree-matched Prüfer nulls preserve degrees but destroy this recursive causal/birth-order structure.

This suggests that the next test should isolate the recursive-order constraint itself before searching additional scalar topology metrics.

## 9. Frozen ledger

```text
T46_PARENT = T45
T46_HOLDOUT_N = 2048
T46_HOLDOUT_SEEDS = {389,419,449}

T46_BASELINE_FEASIBILITY = PASS_3_OF_3
T46_INTERVENTION_FEASIBILITY = PASS_3_OF_3
T46_PB_MATCH_CONTROL = PASS

T46_M3_AFTER_PB = HIGH_3_OF_3
T46_M7_AFTER_PB = HIGH_3_OF_3

T46_HIGH_RADIUS_ACCOUNTING = NOT_IDENTIFIED
T46_RESIDUAL_SPECIFICITY_AFTER_PB = IDENTIFIED_3_OF_3

T46_PB_STATUS = STRUCTURALLY_RELEVANT_BUT_INSUFFICIENT

T46_RECURSIVE_BIRTH_ORDER = NOT_CONTROLLED_BY_PRIOR_NULLS
T46_PHYSICAL_GEOMETRY = NOT_DERIVED
T46_GRAPH_EVOLUTION = NONE
T46_TRANSITION_LAW = NOT_DERIVED
```

## 10. Consequence

The next clean falsification is not another scalar metric.

Construct degree-preserving **recursive-order null trees** that retain:

```text
parent(v) < v for every v>0
```

while randomizing which allowed older parent receives each child.

This directly tests whether the replicated M3/M7 structure is generated by the causal/ancestral recursive organization itself.

Only if the residual survives that null should the five-candidate minimum-degree parent-selection rule be isolated next.
