# v0.5 T6 — Multidirectional Resolutive Connection and Curvature

**Line:** v0.5 relational ontology — theory-only track  
**Depends on:** T1-T5  
**Experimental line:** E0/E1 FROZEN  
**Status:** CONDITIONAL GEOMETRIC REPRESENTATION DERIVATION  
**Target-data fitting:** NONE

## 1. Purpose

T5 established that a differentiable one-parameter representation admits a local generator element `G_R(lambda)d_lambda` and that noncommuting generators preserve ordering information. T6 asks what additional assumptions are required to compare local Resolutive transport along more than one independent represented direction and whether a curvature-like loop defect follows.

No physical spacetime, metric, gravity, phase, energy or legacy RSMS structure is assumed.

## 2. New assumptions declared at T6

On a selected represented sector, assume:

```text
B1: there exists a local differentiable base X with coordinates x^a;
B2: represented internal objects/fibers can be compared by local transport;
B3: each coordinate direction has a local generator A_a(x);
B4: local basis/frame changes are invertible and differentiable;
B5: transport respects composition inherited from C_R.
```

The base `X` is initially only a representation base. It is not identified with physical space or spacetime.

## 3. Resolutive Connection Element

Define the operator-valued one-form-like object

```text
A_R = A_a(x) dx^a.
```

Along a represented curve `x^a(lambda)`, the T5 generator is recovered by pullback:

```text
G_R(lambda)
 = A_a(x(lambda)) dx^a/dlambda.
```

Hence

```text
G_R(lambda)d_lambda
 = A_a dx^a.
```

This gives a consistent multidirectional extension of the T5 Generator Element.

Terminology:

```text
A_R = Resolutive Connection Element
```

conditionally within the represented bundle/transport structure.

## 4. Local transport

For an infinitesimal displacement `dx^a`, write

```text
U(dx) = I + A_a dx^a + O(dx^2).
```

Finite transport along a curve is represented by the path-ordered exponential

```text
U_Gamma
 = P exp integral_Gamma A_R.
```

This is inherited from T5 after pulling the multidirectional connection onto the curve.

## 5. Frame transformation

Under a local invertible frame change

```text
B(x),
```

the connection transforms, for the convention used in T5, as

```text
A'_a
 = B A_a B^-1
   + (partial_a B) B^-1.
```

The inhomogeneous derivative term means `A_a` itself is not generally a frame-invariant observable.

This transformation law is forced by demanding that represented transport transform consistently between local frames.

## 6. Infinitesimal rectangular loop

Consider a small loop in represented directions `a,b` with side lengths `epsilon` and `delta`:

```text
+x^a -> +x^b -> -x^a -> -x^b.
```

Expanding the four local transports consistently through second order yields

```text
U_square
 = I + epsilon delta F_ab + higher-order terms,
```

up to orientation/sign convention, where

```text
F_ab
 = partial_a A_b
 - partial_b A_a
 + [A_a,A_b].
```

Thus the loop defect contains both:

```text
variation between directions
+
noncommutativity of local generators.
```

## 7. Resolutive Curvature

Within the explicitly assumed multidirectional transport representation, define

```text
F_R
 = dA_R + A_R wedge A_R.
```

In components:

```text
F_ab
 = partial_a A_b
 - partial_b A_a
 + [A_a,A_b].
```

Terminology:

```text
F_R = Resolutive Curvature of the represented connection
```

This is now mathematically legitimate because T6 explicitly introduced the connection-like transport structure that T5 lacked.

Critical boundary:

```text
RESOLUTIVE_CURVATURE != SPACETIME_CURVATURE
RESOLUTIVE_CURVATURE != GRAVITY
```

No metric or Levi-Civita connection has been introduced.

## 8. Gauge/frame covariance of curvature

Using the T6 frame transformation, the curvature transforms homogeneously:

```text
F'_ab = B F_ab B^-1
```

for the adopted convention.

Therefore raw matrix entries remain frame-dependent, while conjugation-invariant functions/classes of `F` can provide representation-invariant candidates.

This is the first natural route in v0.5 from local generators to invariant local information.

## 9. Flatness

Define a represented sector as flat when

```text
F_R = 0.
```

Then infinitesimal loop transport is identity to the leading enclosed-area order.

However, local flatness does not by itself imply globally trivial transport if the represented base has nontrivial topology.

Therefore T6 separates:

```text
LOCAL_CURVATURE
from
GLOBAL_HOLONOMY/TOPOLOGY.
```

The latter requires a topology audit.

## 10. Relation to T2 loops

T2 treated closed Resolutive trajectories algebraically. T6 now provides a represented local mechanism by which a small loop can differ from identity:

```text
T2: loop != identity algebraically
T6: U_loop - I ~ F_ab area
```

This does not prove every nontrivial T2 loop is curvature-generated. Global/topological and nonlocal algebraic distinctions may remain.

## 11. Bianchi-type structural identity

Once a connection and curvature are defined, introduce the covariant derivative schematically as

```text
D = d + [A_R, .]
```

with convention-dependent signs.

The curvature satisfies the geometric identity

```text
D F_R = 0
```

(the Bianchi identity) under the standard differential-form construction.

This is a structural identity, not a dynamical field equation and not a conservation law of matter/energy.

```text
BIANCHI_IDENTITY != PHYSICAL_DYNAMICS
```

## 12. Can a scalar be formed without a metric?

Not canonically from `F_ab` alone.

Expressions such as

```text
F_ab F^ab
```

require a rule for raising indices, normally a metric or equivalent pairing on the base.

A trace such as

```text
Tr(F wedge F)
```

may be defined only after choosing a suitable representation/invariant bilinear form and appropriate base dimension/differential structure.

Therefore:

```text
T6_CANONICAL_SCALAR_CURVATURE = NOT_DERIVED
T6_ACTION_DENSITY = NOT_DERIVED
```

## 13. Metric status

No metric follows from the connection structure alone.

A metric candidate would require an independent rule assigning relational separation/inner products or another structure from which a metric can emerge.

Thus:

```text
T6_METRIC = NOT_DERIVED
```

and there is still no legitimate Einstein-Hilbert-like action.

## 14. Topology and periodicity

T6 finally makes global transport/holonomy mathematically meaningful once a topology is supplied to the represented base and bundle.

But topology itself was introduced only conditionally at the representation level. Consequently:

```text
HOLONOMY = AVAILABLE_AFTER_GLOBAL_STRUCTURE
WINDING = REQUIRES_TOPOLOGY
2PI = NOT_DERIVED
4PI = NOT_DERIVED
```

A 4pi behavior could later arise from a double-cover/spinorial representation, but T6 provides no reason yet to select such a representation.

## 15. Relation to old Resolutive phase/coherence language

T6 provides a legitimate mathematical place where a future notion of relative orientation/coherence could be defined: comparison by parallel transport between represented internal states.

However, the old variables `Theta_R`, phase alignment, horizontal/vertical coherence and 4pi transfer rules are not imported.

If any are recovered, they must be mapped onto invariant transport/holonomy structures and shown to follow from additional independently justified assumptions.

## 16. Frozen T6 result

```text
T6_MULTIDIMENSIONAL_BASE = ASSUMED_AT_REPRESENTATION_LEVEL
T6_CONNECTION_ELEMENT_A_R = DERIVED_CONDITIONALLY
T6_FRAME_TRANSFORMATION = DERIVED
T6_CURVATURE_F_R = DERIVED_CONDITIONALLY
T6_INFINITESIMAL_LOOP_DEFECT = DERIVED
T6_CURVATURE_COVARIANCE = DERIVED
T6_BIANCHI_IDENTITY = AVAILABLE_STRUCTURALLY
T6_METRIC = NOT_DERIVED
T6_SCALAR_ACTION = NOT_DERIVED
T6_SPACETIME = NOT_DERIVED
T6_GRAVITY = NOT_DERIVED
T6_ENERGY = NOT_DERIVED
T6_TIME = NOT_DERIVED
T6_4PI = NOT_DERIVED
```

## 17. Main theoretical result

Under the minimal added assumption that the continuous representation possesses multiple differentiable directions and composable local transport, the ordered-history structure of T2-T5 admits a connection whose infinitesimal closed-loop defect is curvature:

```text
A_R = A_a dx^a
F_R = dA_R + A_R wedge A_R.
```

This curvature is not inserted as a phenomenological term; it follows conditionally from the geometry of the chosen transport representation.

The novelty claim must remain modest: this is standard connection/curvature mathematics applied as a disciplined representation of the Resolutive ontology.

## 18. Next theoretical gate — T7: Metric and Relational Separation Audit

The next missing structure is not another field equation. It is a rule for relational magnitude/separation.

T7 should test whether a metric-like object can be justified from the existing ontology/representation or whether it must be an independent postulate.

Questions:

1. Can distinguishability of nearby represented states induce a positive/semi-positive quadratic form?
2. Can composition or symmetry select an invariant bilinear form?
3. Is a metric on the representation base necessary, or only an internal pairing?
4. What assumptions distinguish Euclidean, Lorentzian or indefinite signatures?
5. Can any scalar curvature/action be formed before a metric is physically selected?
6. Does the old Resolutive density concept have any legitimate route to a measure/metric density without being imported?