# v0.4 Fundamental Dynamics Roadmap

**Status:** CLOSED ROADMAP — HISTORICAL RECONSTRUCTION RECORD — ISSUE #84

## Purpose

This document records the reconstruction path used to test whether the old RSMS formulation could yield a unique Resolutive Master Equation without tuning against the v0.3/v0.4 cosmological residuals.

The cosmological RCMS surrogate remained historically frozen throughout this work.

## Normative primitive state

The reconstruction is now reconciled to the RSMS-1.0 primitive state:

```text
R = (rho_R, Theta_R, iota_R, kappa_R).
```

Define

```text
phi_R = Theta_R / 2,
Psi = sqrt(rho_R) exp(i phi_R).
```

The earlier exploratory variables

```text
chi,
n in S^2
```

were used only in preliminary candidate formulations. They are **not** RSMS universal primitives. No mapping from `(chi,n)` to `(iota_R,kappa_R)` is defined, so they must not be presented as the current state of the theory.

## Classification vocabulary

Statements in this reconstruction use:

- **D** — Derived;
- **P** — Postulated;
- **C** — Conjectural;
- **K** — Computational implementation;
- **O** — Observational input;
- **F** — Falsifiable prediction.

No analogy is promoted to physical law without an explicit mathematical step.

## Reconstruction chain tested

The audit proceeded through:

1. primitive state variables;
2. state-space geometry;
3. periodicity/topology assumptions;
4. field content;
5. action/evolution-law candidates;
6. Euler-Lagrange/evolution equations for the minimal baseline;
7. symmetries and conservation laws;
8. bundle/connection/transport structure;
9. observable-projection requirements;
10. principle-selection tests P-A through P-E.

## 4pi result

The half-angle representation was explicitly audited.

```text
Theta_R in R/(4pi Z) <-> phi_R in R/(2pi Z)
```

is a smooth coordinate equivalence. Therefore:

```text
FOUR_PI_STATUS = PARAMETRIZATION_ONLY_4PI
```

for the current scalar phase representation.

Physical `4pi` behavior would require additional independently justified state/topological/transport structure plus an observable discriminator. No such structure was derived in this cycle.

## Conventional core

For the audited core

```text
B0 = (rho_R, phi_R),
```

with

```text
Psi = sqrt(rho_R) exp(i phi_R),
```

the standard complex-scalar kinetic term becomes

```text
|partial Psi|^2
= (partial rho_R)^2/(4 rho_R)
+ rho_R (partial phi_R)^2.
```

Equivalently, with `Theta_R = 2 phi_R`,

```text
|partial Psi|^2
= (partial rho_R)^2/(4 rho_R)
+ rho_R (partial Theta_R)^2/4.
```

Thus:

```text
NOVELTY_(rho_R,phi_R) = EQUIVALENT_REPARAMETRIZATION
```

for this kinetic core.

## Unresolved RSMS extension sector

The normative unresolved sector is

```text
X_R = (iota_R, kappa_R).
```

Current RSMS does not uniquely fix for either variable:

- exact physical interpretation;
- complete domain/equivalence structure;
- units;
- transformation law;
- dynamics;
- coupling to `(rho_R,phi_R)`;
- observable projection.

Generic free-scalar kinetic terms or arbitrary potentials do not count as derivations and would only produce conventional multi-scalar model freedom.

## Anti-retuning rule

The following remained forbidden:

- choosing terms because they improve already-seen RCMS cosmology residuals;
- inserting redshift switches, survey coefficients, or dataset-specific amplitudes;
- using the failed held-out BOSS result to choose a new functional form;
- treating earlier favorable SPARC/cosmology fits as derivation evidence;
- adding hidden state solely to restore physical `4pi`;
- importing a connection, SU(2) lift, chirality bit, or holonomy merely because it can mathematically produce two-step closure.

## Audit outcomes

The following routes were tested and did not uniquely select dynamics:

```text
P-A variational principle            = REJECT_UNDERDETERMINED
P-B geometric transport/holonomy     = REJECT_UNDERDETERMINED
P-C invariance/conservation          = REJECT_UNDERDETERMINED
P-D old-formulation state transition = REJECT_UNDERDETERMINED
P-E coherence/information extremum   = REJECT_UNDERDETERMINED
```

Therefore the frozen protocol stop rule activated:

```text
PRINCIPLE_SEARCH = NO_UNIQUE_SELECTOR_FOUND
OLD_FORMULATION_SELECTOR_CYCLE = CLOSED_NEGATIVE
```

## Gate outcome

```text
G1 STATE_SPACE_EXPLICIT   = PARTIAL
G2 FOUR_PI_CLASSIFIED     = PASS_CLASSIFIED_CURRENT_MODEL
G3 DYNAMICS_EXPLICIT      = PASS_FOR_CONVENTIONAL_CORE / FAIL_FOR_FULL_RSMS
G4 NOVELTY_CLASSIFIED     = EQUIVALENT_CORE / UNDERDETERMINED_FULL_STATE
```

A unique Resolutive Master Equation cannot be promoted under the current gates.

## Final roadmap decision

This roadmap is no longer an open instruction to continue searching inside the same formulation. Its final result is:

```text
MASTER_EQUATION_STATUS = NOT_UNIQUELY_DERIVED_FROM_CURRENT_RSMS
FUNDAMENTAL_NOVELTY_STATUS = UNDERDETERMINED
CURRENT_CONVENTIONAL_CORE = (rho_R, phi_R)
CURRENT_4PI_STATUS = PARAMETRIZATION_ONLY_4PI
V0_4_FUNDAMENTAL_RECONSTRUCTION = DIAGNOSTIC_STOP_UNDERDETERMINED
OLD_FORMULATION_SELECTOR_CYCLE = CLOSED_NEGATIVE
```

No new cosmological or galactic fit is authorized by this roadmap.

Any future fundamental formulation must be opened separately, preserve this negative result, and declare its primitives, mathematical novelty, observable discriminator, null comparator, forbidden formulation data, and failure criterion before target-data evaluation.