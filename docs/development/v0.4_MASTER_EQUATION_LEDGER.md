# v0.4 Master Equation Ledger

**Status:** WORKING CLASSIFICATION — RSMS PRIMITIVES RECONCILED

This ledger records the current candidate ingredients without promoting unproven assumptions to physical law.

## 1. Primitive set

The v0.4 reconstruction shall follow the RSMS-1.0 primitive state:

```text
R = (rho_R, Theta_R, iota_R, kappa_R)
```

The earlier exploratory variables `chi` and `n in S^2` are not RSMS universal primitives and are removed from the universal ledger. They may return only as derived or specialization-specific quantities with an explicit mapping and independent justification.

| ID | Element | Mathematical form / domain | Class | Current status | Required evidence |
|---|---|---|---|---|---|
| ME-01 | Resolutive density/depth | `rho_R(x) >= 0` | P | RSMS primitive; physics unresolved | Units, physical interpretation, observable map |
| ME-02 | Resolutive phase | `Theta_R(x)` | P | RSMS primitive | Exact quotient/equivalence relation |
| ME-03 | Resolutive inclination | `iota_R(x) in D_iota subset R` | P | RSMS primitive; physics unresolved | Domain, units, transformation law, dynamics, observable effect |
| ME-04 | Local resolutive curvature | `kappa_R(x) in D_kappa subset R` | P | RSMS primitive; physics unresolved | Domain, units, relation or nonrelation to geometry, dynamics, observable effect |
| ME-05 | Complex baseline representation | `Psi = sqrt(rho_R) exp(i Theta_R/2)` | D from ME-01/02 once defined | Representation only | Half-angle audited in #85 |
| ME-06 | `4pi` periodicity of current scalar representation | `Theta_R ~ Theta_R + 4pi` | D as coordinate choice, not new physical law | **PARAMETRIZATION_ONLY_4PI** | New physical structure would require independent derivation + observable |
| ME-07 | Horizontal `2pi` rule | same-sector phase evolution closes at `2pi` in `phi_R=Theta_R/2` | D for ordinary `U(1)` | Conventional closure only | Any special Resolutive meaning needs separate derivation |
| ME-08 | Vertical/inter-layer `4pi` rule | proposed inter-layer transfer requires `4pi` | C | **NOT DERIVED** | Independently required lifted state/transition law and observable |
| ME-09 | Baseline density kinetic term | `(partial rho_R)^2/(4 rho_R)` | D algebraically from complex-scalar polar form; P as physical law | Conventional baseline | Metric, dimensions, coefficient convention |
| ME-10 | Baseline phase kinetic term | `rho_R (partial Theta_R)^2/4` | D algebraically from complex-scalar polar form; P as physical law | Conventional baseline | Metric, dimensions, coefficient convention |
| ME-11 | Inclination dynamics | TBD | P/C | **UNDEFINED** | Must not default silently to a free scalar kinetic term |
| ME-12 | Curvature-state dynamics | TBD | P/C | **UNDEFINED** | Must not be identified with spacetime curvature without a mapping |
| ME-13 | Coupling/potential | `V(rho_R, phi_R, iota_R, kappa_R)` or alternative direct law | C/P | **UNDEFINED** | Minimal symmetry-respecting form fixed before fitting |
| ME-14 | Candidate Master Equation | derived only after ME-11..13 and dimensions close | C/P | **NOT MASTER EQUATION YET** | E-L/evolution equations, symmetries, limits, novelty audit |
| ME-15 | Cosmological RCMS surrogate | `E^2 = Omega_m(1+z)^3 + (1-Omega_m) + A_R ln(1+z)` | K/F historical screening model | Frozen historical line | Must not be used to derive/tune ME-01..14 |

## 2. Resolved 4pi state-space audit

Define

```text
phi_R = Theta_R/2.
```

Then

```text
Theta_R in R/(4pi Z) <-> phi_R in R/(2pi Z),
```

with a smooth bijection and smooth inverse. Therefore the bare `4pi` coordinate is diffeomorphic to an ordinary phase circle.

From

```text
Psi = sqrt(rho_R) exp(i Theta_R/2),
```

we obtain

```text
Psi(Theta_R + 2pi) = -Psi(Theta_R),
Psi(Theta_R + 4pi) =  Psi(Theta_R),
```

which in `phi_R` is simply the ordinary transformation

```text
Psi(phi_R + pi)  = -Psi(phi_R),
Psi(phi_R + 2pi) =  Psi(phi_R).
```

Result:

```text
FOUR_PI_STATUS = PARAMETRIZATION_ONLY_4PI
```

for the current scalar representation.

## 3. Kinetic-sector equivalence

Direct differentiation gives, up to metric/sign convention,

```text
|partial Psi|^2
= (partial rho_R)^2/(4 rho_R)
  + rho_R (partial Theta_R)^2/4
= (partial rho_R)^2/(4 rho_R)
  + rho_R (partial phi_R)^2.
```

Therefore ME-09 and ME-10 are exactly the ordinary polar decomposition of a conventional complex scalar kinetic sector.

```text
NOVELTY_(rho_R,phi_R) = EQUIVALENT_REPARAMETRIZATION
```

## 4. Layer-route audit

Previous tests gave:

```text
n in S^2                 -> no intrinsic nontrivial continuous global Z2 layer
chirality in O(3)        -> genuine Z2, but proper 2pi rotations preserve it
SU(2)/quaternion lifting -> genuine angular double cover, but lift sign is not observable by current fields
```

None derives the proposed physical vertical `4pi` rule from the current RSMS state.

Orientation and chirality remain possible derived/specialized quantities, not universal primitives.

## 5. RSMS reconciliation result

RSMS-R015 fixes exactly four primitive components. Therefore the prior exploratory state

```text
(rho, Theta, chi, n)
```

was not aligned with the normative RSMS state.

The corrected unresolved extension sector is

```text
X_R = (iota_R, kappa_R).
```

Their specification status is stronger than an arbitrary invented field, but their **physical content is still unvalidated and underdefined**. Merely adding conventional kinetic terms for them would produce an ordinary multi-scalar extension and would not establish Resolutive novelty.

The detailed reconciliation is recorded in:

```text
docs/development/v0.4_RSMS_PRIMITIVE_RECONCILIATION.md
```

## 6. Dimensional audit — pending

| Quantity | Domain | Units | Status |
|---|---|---|---|
| `x^mu` | spacetime/domain coordinate | TBD | OPEN |
| `rho_R` | `R_+` / declared nonnegative domain | TBD | OPEN |
| `Theta_R` | angular coordinate | dimensionless | provisional |
| `phi_R` | angular coordinate | dimensionless | provisional |
| `iota_R` | real scalar coordinate | TBD; dimensionless only if independently established as angle | OPEN |
| `kappa_R` | real scalar coordinate | TBD | OPEN |
| `V` / interaction law | model-dependent | TBD | OPEN |

No dimensional validation claim is permitted until this table closes.

## 7. Acceptance gates

### Gate G1 — STATE_SPACE_EXPLICIT

Pass only when:

- domains of `rho_R`, `Theta_R`, `iota_R`, and `kappa_R` are frozen;
- the phase quotient relation is explicit;
- units and physical equivalences are declared;
- the physical roles of `iota_R` and `kappa_R` are specified sufficiently to distinguish state from coordinate convention.

Current state: **FAIL_OPEN**.

### Gate G2 — FOUR_PI_CLASSIFIED

Current result:

```text
PARAMETRIZATION_ONLY_4PI
```

Gate result: **PASS_CLASSIFIED_CURRENT_MODEL**.

A future independent physical structure may reopen the question only as a new preregistered extension.

### Gate G3 — DYNAMICS_EXPLICIT

The `(rho_R,phi_R)` baseline has explicit conventional dynamics in `v0.4_MINIMAL_DYNAMICS_AUDIT.md`, but the RSMS-native variables `iota_R` and `kappa_R` have no frozen dynamics.

Current state: **PARTIAL / FAIL_FULL_MODEL**.

### Gate G4 — NOVELTY_CLASSIFIED

```text
(rho_R,phi_R) sector -> EQUIVALENT_REPARAMETRIZATION
full RSMS state      -> UNDERDETERMINED
```

Gate state: **PARTIAL**.

## 8. Anti-retuning rule

The following remain forbidden during fundamental reconstruction:

- choosing an `iota_R` or `kappa_R` coupling because it improves already-seen RCMS residuals;
- importing the RCMS reduced scalar `S_R` upstream as if it were derived;
- using the failed BOSS held-out result to choose a new interaction;
- adding hidden states solely to recover `4pi`;
- treating earlier favorable cosmological or galactic fits as derivation evidence.

RCMS may be used later only as historical/independent test material after the upstream dynamics is frozen.

## 9. Immediate next derivation

The next task is a strict RSMS-native extension audit:

1. test `iota_R` alone against the conventional `(rho_R,phi_R)` baseline;
2. test `kappa_R` alone against the same baseline;
3. require each candidate to have a frozen domain, transformation law, units, dynamics and observable projection;
4. reject any extension that is merely an arbitrary free scalar or a coordinate relabeling;
5. only if one survives, derive the coupled equations and a preregistered observable discriminator.

No new cosmological or galactic fit is authorized yet.
