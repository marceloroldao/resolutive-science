# v0.5 T41 — Higher-Order Matched Tree Null Results

**Protocol:** `v0.5_T41_HIGHER_ORDER_MATCHED_TREE_NULL_PROTOCOL.md`  
**Parent:** T40. **New dynamics:** none.  
**Held-out responses:** M3 and M7.

## 1. Executive result

T41 successfully generated the full preregistered higher-order matched null ensemble:

```text
99 unique null trees x 9 parents = 891 matched nulls.
```

Every stored null preserves:

- N;
- exact labeled-node degree sequence;
- tree class;
- the frozen C/A/G matching box;
- the frozen diversity rule.

M3 and M7 were not used by the sampler.

Therefore all control gates pass.

The held-out outcome is mixed:

- both M3 and M7 remain above q97.5 in 5/9 parents;
- both fall inside the matched-null 95% interval in 2/9 parents;
- one response remains high and the other falls inside in 2/9 parents.

Thus neither full accounting nor all-9 residual specificity is identified.

```text
T41_HIGHER_ORDER_ACCOUNTING = NOT_IDENTIFIED
T41_RESIDUAL_HIGH_RADIUS_SPECIFICITY = NOT_IDENTIFIED_ALL_9
T41_OUTCOME = MIXED_PARTIAL_ACCOUNTING_WITH_RESIDUAL_SPECIFICITY
```

A notable descriptive pattern is that all three N=512 parents retain high M3 and high M7 after matching. This size pattern was not a preregistered T41 gate and is not promoted without a new protocol.

## 2. Control gates

All 9 parents produced 99 unique matched nulls before the frozen proposal ceiling of 250000.

Proposal counts required to complete each ensemble:

| N | seed 11 | seed 23 | seed 47 |
|---:|---:|---:|---:|
| 128 | 9647 | 9941 | 10456 |
| 256 | 10046 | 9642 | 11416 |
| 512 | 9793 | 9344 | 9604 |

The walk reaches the required

```text
5450 accepted moves
= 500 burn-in + 99 x 50 thinning
```

for every parent.

Therefore:

```text
T41_MATCH_FEASIBILITY = PASS_9_OF_9
T41_DEGREE_CONTROL = PASS_EXACT
T41_HIGHER_ORDER_MATCH_CONTROL = PASS
T41_DIVERSITY_CONTROL = PASS
```

## 3. Diversity

The preregistered parent-edge-overlap ceiling was 0.90.

The realized matched nulls are far more distinct than required.

Maximum observed parent-edge retention by ensemble is approximately:

```text
N=128: <= 0.071
N=256: <= 0.035
N=512: <= 0.029
```

Mean overlap is approximately 0.5%–2.1% depending on N/seed.

Thus the matched trees are not small perturbative copies of the parent adjacency.

## 4. Held-out M3/M7 classification

| N | seed | M3 parent | M3 q025–q975 | class | M7 parent | M7 q025–q975 | class |
|---:|---:|---:|---:|:---:|---:|---:|:---:|
| 128 | 11 | 0.6538 | 0.5806–0.6417 | HIGH | 0.1915 | 0.1560–0.1875 | HIGH |
| 128 | 23 | 0.6283 | 0.5882–0.6619 | inside | 0.1760 | 0.1580–0.1980 | inside |
| 128 | 47 | 0.6522 | 0.6216–0.6772 | inside | 0.1943 | 0.1636–0.2000 | inside |
| 256 | 11 | 0.6525 | 0.6188–0.6667 | inside | 0.1996 | 0.1718–0.1965 | HIGH |
| 256 | 23 | 0.6637 | 0.6194–0.6577 | HIGH | 0.1935 | 0.1720–0.2031 | inside |
| 256 | 47 | 0.6923 | 0.6429–0.6790 | HIGH | 0.2228 | 0.1811–0.2099 | HIGH |
| 512 | 11 | 0.6667 | 0.6244–0.6560 | HIGH | 0.2139 | 0.1776–0.1946 | HIGH |
| 512 | 23 | 0.6575 | 0.6250–0.6503 | HIGH | 0.2074 | 0.1776–0.2016 | HIGH |
| 512 | 47 | 0.6667 | 0.6230–0.6538 | HIGH | 0.2231 | 0.1801–0.1975 | HIGH |

Counts:

```text
M3 HIGH = 6/9
M7 HIGH = 6/9
M3 and M7 both HIGH = 5/9
M3 and M7 both inside = 2/9
mixed one-high/one-inside = 2/9
```

## 5. Main inference

The T39/T40 structural family

```text
C = global compactness
A = degree assortativity
G = betweenness concentration
```

is neither irrelevant nor sufficient.

It is relevant because higher-order matching moves some previously extreme parent responses into the matched-null distribution.

It is insufficient because substantial held-out specificity remains in multiple parents after exact degree control and C/A/G matching.

Therefore:

```text
C,A,G = PARTIAL_STRUCTURAL_ACCOUNTING
C,A,G = NOT_COMPLETE_STRUCTURAL_ACCOUNTING
```

## 6. Size pattern — descriptive only

The held-out pattern becomes more consistently residual at the largest tested N:

```text
N=128:
  both HIGH = 1/3
  both inside = 2/3

N=256:
  both HIGH = 1/3
  mixed = 2/3

N=512:
  both HIGH = 3/3
```

This suggests a possible finite-size/convergence question:

> Does residual M3/M7 specificity strengthen rather than disappear as N increases after higher-order topology matching?

T41 did not preregister a size-trend gate, so this observation is hypothesis-generating only.

## 7. What T41 rules out

The T38 result can no longer be attributed simply to:

- tree class;
- degree sequence;
- ordinary random higher-order topology;
- or the coarse C/A/G family alone.

At the same time, T41 also rules out the stronger claim that an additional invariant is already proven universally, because 4/9 parents fail the both-high residual pattern.

## 8. Frozen ledger

```text
T41_PARENT = T40

T41_MATCH_FEASIBILITY = PASS_9_OF_9
T41_DEGREE_CONTROL = PASS_EXACT
T41_HIGHER_ORDER_MATCH_CONTROL = PASS
T41_DIVERSITY_CONTROL = PASS

T41_M3_HIGH = 6_OF_9
T41_M7_HIGH = 6_OF_9
T41_BOTH_HIGH = 5_OF_9
T41_BOTH_INSIDE = 2_OF_9
T41_MIXED = 2_OF_9

T41_HIGHER_ORDER_ACCOUNTING = NOT_IDENTIFIED
T41_RESIDUAL_HIGH_RADIUS_SPECIFICITY = NOT_IDENTIFIED_ALL_9

T41_CAG_ACCOUNTING = PARTIAL
T41_ADDITIONAL_INVARIANT = SUGGESTED_NOT_DERIVED

T41_SIZE_TREND = POSTHOC_DESCRIPTIVE_ONLY

T41_PHYSICAL_GEOMETRY = NOT_DERIVED
T41_GRAPH_EVOLUTION = NONE
T41_TRANSITION_LAW = NOT_DERIVED
```

## 9. Consequence

The next scientifically admissible gate is a preregistered size/convergence audit.

It should hold the T41 matching construction fixed and increase N beyond 512 using new parent seeds fixed before execution.

The key question should be:

> After matching exact degree sequence and C/A/G, does the joint held-out residual of M3/M7 tend toward zero, remain finite, or strengthen with increasing N?

No new topology variable should be introduced in that test.

If residual specificity strengthens with N, the next stage may search for the missing invariant.

If it decays, the current residual is likely a finite-size structural effect.

No physical dynamics should be introduced before resolving that limit behavior.
