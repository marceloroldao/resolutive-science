# v0.5 T14 — First Reference Simulator Results

**Line:** v0.5 relational ontology — computational consistency track
**Depends on:** T13 and `src/v05/t14_reference_simulator.py`
**Status:** FIRST EXECUTED REFERENCE RESULTS
**Physical validation:** NONE
**Legacy-constant search:** NONE

## 1. Scope

This record freezes the first execution of the T14 reference consistency suite. These are mathematical/computational checks of the candidate implementations. They are not evidence that the candidate laws describe nature.

The reference implementation was reproduced in an isolated Python execution using the same equations, seeds and parameters encoded by the simulator.

## 2. D13-A exact reversibility

Parameters:

```text
samples = 500
n = 7
m = 5
seed = 13
```

Result:

```text
reconstruction failures = 0 / 500
PASS
```

For every sampled pair `(x_{k-1}, x_k)`, the implemented inverse recovered `x_{k-1}` exactly from `(x_k, x_{k+1})`.

Interpretation: the tested implementation satisfies its intended algebraic reversibility property for these samples. The formula itself provides the analytic inversion, so the numerical test is an implementation check rather than a proof of physical reversibility.

## 3. D13-A permutation covariance

Parameters:

```text
samples = 250
n = 7
m = 5
seed = 14
```

Result:

```text
covariance failures = 0 / 250
PASS
```

Arbitrary node relabelings, with the relational neighborhood relabeled consistently, commuted with the tested update.

Interpretation: the implementation did not depend on the arbitrary node names in the sampled tests.

## 4. D13-A exhaustive small-state orbit scan

Parameters:

```text
n = 3
m = 2
instantaneous states = 2^3 = 8
second-order pair states = 64
```

Every one of the 64 pair states returned to its initial pair state within the exhaustive finite-state bound.

Observed period histogram:

```text
period 1 :  2 pair states
period 2 :  2 pair states
period 3 : 30 pair states
period 6 : 30 pair states
missing  :  0
```

This distribution was not fitted to any legacy Resolutive periodicity.

Important: periods 1, 2, 3 and 6 are properties of this tiny chosen rule/topology/parameter instance. They are not physical constants and must not be interpreted post hoc as evidence for an old numerical hypothesis.

## 5. D13-B controlled history ablation

Parameters:

```text
n = 7
m = 5
history modulus = 7
```

The same instantaneous state was evolved with two different controlled bounded history labels.

Results:

```text
next state, history A = (4, 3, 0, 2, 4, 1, 0)
next state, history B = (0, 0, 3, 1, 4, 2, 0)
differing sites = 5 / 7
PASS (implementation sensitivity)
```

Interpretation: D13-B successfully implements history-sensitive dynamics by construction. This is not evidence that physical systems require such a history variable. A future physical test must compare predictive performance after controlling for omitted instantaneous variables.

## 6. D13-C normalization

Parameters:

```text
samples = 250
n = 7
m = 5
seed = 15
```

Result:

```text
maximum |sum(P)-1| = 1.1102230246251565e-16
PASS
```

The residual is at floating-point roundoff scale.

Interpretation: the tested local stochastic kernel is numerically normalized.

## 7. What has been established

```text
T14_A_IMPLEMENTATION_REVERSIBILITY = PASS
T14_A_PERMUTATION_COVARIANCE = PASS
T14_A_SMALL_FINITE_ORBITS = PASS
T14_B_HISTORY_SENSITIVITY_IMPLEMENTED = PASS
T14_C_KERNEL_NORMALIZATION = PASS
```

## 8. What has NOT been established

```text
PHYSICAL_REVERSIBILITY = NOT TESTED
PHYSICAL_HISTORY_DEPENDENCE = NOT TESTED
FUNDAMENTAL_STOCHASTICITY = NOT TESTED
CONTINUUM_LIMIT = NOT DERIVED
PHYSICAL_CURVATURE = NOT DERIVED
PHYSICAL_TIME = NOT MEASURED
ENERGY = NOT DERIVED FROM SIMULATION
4PI = NOT TESTED/NOT INSERTED
LEGACY CONSTANTS = NOT SEARCHED
```

## 9. First mathematical observation

The small reversible model generates nontrivial finite orbit orders without inserting a preferred periodicity. In the `n=3, m=2` instance, the dominant pair-state periods are 3 and 6, not a universal order 2.

This supports the T9 discipline: finite return order should be treated as an emergent property of a specified representation/rule, not fixed universally in advance.

It does not establish that periods 3 or 6 have physical significance.

## 10. Gate to T15

The next computational gate is to test whether independently defined reversible relational perturbations/updates fail to commute and whether a discrete closed-loop defect survives controlled coarse-graining.

T15 should implement at least two distinct invertible local operations `U_a` and `U_b` and evaluate

```text
Delta_ab = U_a U_b U_a^-1 U_b^-1.
```

Required comparisons:

```text
commuting control pair -> Delta_ab = I
noncommuting candidate pair -> quantify Delta_ab != I
relabeling covariance
system-size scaling
coarse-grained defect scaling
order sensitivity
```

Only after a controlled small-step/scaling limit may this defect be compared with a continuum commutator or curvature tensor.